apiVersion: v1
kind: Service
metadata:
  name: spark-os-website
  namespace: abconvert
  labels:
    app: spark-os-website
spec:
  type: ClusterIP
  selector:
    app: spark-os-website
  ports:
    - name: http
      port: 80
      targetPort: http
