{
  "name": "spark-os-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Spark OS landing page (spark-os.io) — waitlist lead capture into Supabase.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "vite": "^8.0.16"
  }
}
