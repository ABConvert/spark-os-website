# Copy to .env and fill in. VITE_* values are shipped to the browser (public).
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# Server-side only (migrations/admin). Never expose to the browser.
SUPABASE_SECRET_KEY=sb_secret_xxx
